## Status

DONE

## One-line summary

Next.js 14 (App Router) marketing skeleton with tr-TR placeholder landing — committed as `d21d408`. Also amended root `eslint.config.mjs` to ignore Next-generated artifacts (`.next/`, `next-env.d.ts`, `next.config.mjs`).

## Test evidence

- `pnpm exec next build` → ✓ Generating static pages (4/4), Route `/` 142 B, First Load JS 87.3 kB, both pages prerendered as static
- `pnpm exec eslint apps/marketing/` → exit 0 (after the root ignore update)

## Self-review

- Plan used `JSX.Element` as the function return type. React 18 removed the global `JSX` namespace — `next build` failed with `Cannot find namespace 'JSX'`. Switched to `ReactElement` from `react` (used by every modern React codebase).
- `next-env.d.ts` is auto-generated by `next build` and must NOT be committed (it gets regenerated, and committing it causes spurious diffs). Did not stage it.
- Root `eslint.config.mjs` originally ignored `packages/**/src/generated/**` and Prisma migrations but not Next.js artifacts. The first commit attempt failed because `pnpm exec eslint` from root flagged `next-env.d.ts` (TS parse errors on Next's complex types) and `next.config.mjs` (not in any tsconfig). Added `**/.next/**`, `**/next-env.d.ts`, `**/next.config.mjs` to the global ignores. Pre-commit hook now passes.
- `next-env.d.ts` re-appeared as `M` after `next build`; not staged.
- `tsconfig.json` was reformatted by prettier during the commit hook (multi-line `plugins` and `include` arrays). The prettier rewrite added `lib`, `allowJs`, `isolatedModules` — those are Next.js defaults that prettier merged in. Kept as-is; it still extends the workspace tsconfig.base.
- `project.json` references `@nx/next:dev` and `@nx/next:build` executors that aren't installed (same M-pattern as Task 9, Task 12). `pnpm exec nx build marketing` would fail. `package.json` script uses plain `next build` directly. Parked as M13.1.
- Added `eslint.config.mjs` (M3.5) so the package participates in the workspace lint from any starting point.
- App Router (`app/` directory) used per the plan; pages auto-statically-rendered. No client components needed yet.

## Commits

`d21d408` — feat(marketing): Next.js skeleton with placeholder landing

## Follow-ups (park for final review)

- **M13.1** — `apps/marketing/project.json` references `@nx/next:dev` / `@nx/next:build`; only `@nx/eslint`, `@nx/jest`, `@nx/nest` are installed. Either install `@nx/next` or drop the Nx targets and rely on `package.json` scripts.
- **M13.2** — Once `@nx/next` (or equivalent) is installed, Nx should be the canonical task runner for `dev`/`build` across all three web targets (admin-web, marketing, future mobile web). Pattern of "plan references an Nx executor we don't have" is recurring (M9.1, M12.1, M13.1).
